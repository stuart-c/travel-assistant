/**
 * Database Storage View Controller.
 * Minimalist controller for database metrics display.
 */
document.addEventListener('DOMContentLoaded', () => {
  // Read-only single stat card; no interactive controller required.
});
